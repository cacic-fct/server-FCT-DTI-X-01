---
- name: Manage FCT-DTI-X-01
  hosts: secompp
  become: true
  pre_tasks:
    - name: Load FCT-DTI-X-01 profile variables
      ansible.builtin.include_vars:
        dir: "{{ playbook_dir }}/host_vars/fct-dti-x-01"
        extensions:
          - yml
          - yaml
      tags:
        - always
  roles:
    - role: base
    - role: docker
    - role: secrets
    - role: accounts
    - role: access
    - role: ansible_pull
    - role: compose
