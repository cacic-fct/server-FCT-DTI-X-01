services:
  traefik:
    image: "traefik:v3.7.1"
    container_name: "traefik"
    restart: "always"
    networks:
      - traefik
      - prometheus
    env_file:
      - .env
    command:
      # Lembre-se de comentar novamente após o debugging
      # - "--log.level=DEBUG"

      # Matenha api.insecure=false para não expor a API do Traefik
      - "--api.insecure=false"
      - "--api.dashboard=true"
      - "--providers.docker=true"
      # Mantenha exposedbydefault=false para que o Traefik não exponha todos os containers automaticamente
      # Dessa forma, apenas os containers com a label traefik.enable=true serão expostos
      - "--providers.docker.exposedbydefault=false"
      - "--providers.file.directory=/providers"

      # Setup HTTP
      - "--entrypoints.web.address=:80"
      ## Sempre redireciona para HTTPS
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      ## Middlewares comuns para todos os serviços
      - "--entrypoints.web.http.middlewares=compress@file, ratelimit@file, security-headers@file, crowdsec@file"

      # Setup HTTPS
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.websecure.http.tls.certresolver=letsencrypt"
      - "--entrypoints.websecure.http.tls.domains[0].main=cacic.com.br"
      - "--entrypoints.websecure.http.tls.domains[0].sans=*.cacic.com.br"
      ## HTTP/3
      - "--entrypoints.websecure.http3"
      ## Middlewares comuns para todos os serviços
      - "--entryPoints.websecure.http.middlewares=compress@file, ratelimit@file, security-headers@file, crowdsec@file"
      # https://github.com/traefik/traefik/wiki/respondingTimeouts-for-applications
      - "--entrypoints.websecure.transport.respondingTimeouts.readTimeout=0s"
      ## Confiar nos IPs enviados pelo proxy Cloudflare
      ### https://www.cloudflare.com/ips/
      ### Last updated: Sep 28, 2023
      - "--entryPoints.websecure.forwardedHeaders.trustedIPs=173.245.48.0/20,103.21.244.0/22,103.22.200.0/22,103.31.4.0/22,141.101.64.0/18,108.162.192.0/18,190.93.240.0/20,188.114.96.0/20,197.234.240.0/22,198.41.128.0/17,162.158.0.0/15,104.16.0.0/13,104.24.0.0/14,172.64.0.0/13,131.0.72.0/22,2400:cb00::/32,2606:4700::/32,2803:f800::/32,2405:b500::/32,2405:8100::/32,2a06:98c0::/29,2c0f:f248::/32"

      # Métricas para o Grafana
      - "--metrics.prometheus=true"
      - "--metrics.prometheus.buckets=0.100000, 0.300000, 1.200000, 5.000000"
      - "--metrics.prometheus.addEntryPointsLabels=true"
      - "--metrics.prometheus.addServicesLabels=true"
      - "--entryPoints.metrics.address=:8899"
      - "--metrics.prometheus.entryPoint=metrics"

      # Precisa ser DNS Challenge, pois o proxy do Cloudflare não deixa o HTTP Challenge funcionar
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge.provider=cloudflare"
      - "--certificatesresolvers.letsencrypt.acme.email=letsencrypt@yudi.me"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"

      - "--certificatesresolvers.letsencrypt-ip-http.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt-ip-http.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.letsencrypt-ip-http.acme.profile=shortlived"
      - "--certificatesresolvers.letsencrypt-ip-http.acme.disablecommonname=true"
      - "--certificatesresolvers.letsencrypt-ip-http.acme.email=letsencrypt@yudi.me"
      - "--certificatesresolvers.letsencrypt-ip-http.acme.storage=/letsencrypt/acme-ip.json"

      # Plugins
      ## Crowdsec - Proteção contra ataques
      - "--accesslog"
      - "--accesslog.filepath=/var/log/traefik/access.log"
      - "--experimental.plugins.crowdsec-bouncer-traefik-plugin.modulename=github.com/maxlerebourg/crowdsec-bouncer-traefik-plugin"
      - "--experimental.plugins.crowdsec-bouncer-traefik-plugin.version=v1.6.0"

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "./letsencrypt:/letsencrypt"
      - "./providers:/providers"
      - "/home/shared/docker-data/crowdsec/traefikLogs:/var/log/traefik"
      - "/home/shared/docker-data/cacic-error-pages/html:/error-pages"

    logging:
      driver: "json-file"
      options:
        max-size: "5m"

    labels:
      - "traefik.enable=true"

      # Dashboard
      - "traefik.http.routers.dashboard.entryPoints=websecure"
      - "traefik.http.routers.dashboard.rule=Host(`traefik.cacic.com.br`)"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.tls.certResolver=letsencrypt"
      - "traefik.http.services.dashboard.loadbalancer.server.port=8080"
      - "traefik.http.routers.dashboard.middlewares=authentik@docker, dashboard-redirect"
      ## Redirect acesso sem path para /dashboard
      - "traefik.http.middlewares.dashboard-redirect.redirectregex.regex=^https://traefik.cacic.com.br$$"
      - "traefik.http.middlewares.dashboard-redirect.redirectregex.replacement=https://traefik.cacic.com.br/dashboard"
      - "traefik.http.middlewares.dashboard-redirect.redirectregex.permanent=true"

      # Negar acesso direto pelo IP
      - "traefik.http.routers.unespEntryPointIp.rule=Host(`200.145.181.38`)"
      - "traefik.http.routers.unespEntryPointIp.entrypoints=web, websecure"
      - "traefik.http.routers.unespEntryPointIp.tls=true"
      - "traefik.http.routers.unespEntryPointIp.tls.certResolver=letsencrypt-ip-http"
      - "traefik.http.routers.unespEntryPointIp.tls.options=directIp@file"

      # Redirect 38a.fct.unesp.br e fct-dti-x-01.cacic.com.br para a documentação do servidor
      - "traefik.http.routers.unespEntryPoint.rule=Host(`38a.fct.unesp.br`) || Host(`fct-dti-x-01.cacic.com.br`)"
      - "traefik.http.routers.unespEntryPoint.middlewares=unespEntryPoint-dos, unespEntryPoint-dos-req, redirectToDocs"
      - "traefik.http.routers.unespEntryPoint.entrypoints=web, websecure"
      - "traefik.http.middlewares.redirectToDocs.redirectregex.regex=.*"
      - "traefik.http.middlewares.redirectToDocs.redirectregex.replacement=https://cacic.com.br/docs/Recursos/Servidores/FCT-DTI-X-01"
      - "traefik.http.routers.unespEntryPoint.tls.certResolver=letsencrypt"
      ## Rate limit, por excesso de cautela
      - "traefik.http.middlewares.unespEntryPoint-dos-req.inflightreq.amount=3"
      - "traefik.http.middlewares.unespEntryPoint-dos.ratelimit.average=3"
      - "traefik.http.middlewares.unespEntryPoint-dos.ratelimit.burst=3"
      - "traefik.http.middlewares.unespEntryPoint-dos.ratelimit.sourcecriterion.requestheadername=X-Forwarded-Host"

      # Redirect live.cacic.com.br para cacic.com.br
      - "traefik.http.routers.liveEntryPoint.rule=Host(`live.cacic.com.br`)"
      - "traefik.http.routers.liveEntryPoint.entrypoints=web,websecure"
      - "traefik.http.routers.liveEntryPoint.tls=true"
      - "traefik.http.routers.liveEntryPoint.middlewares=redirectLive"
      - "traefik.http.routers.liveEntryPoint.service=noop@internal"

      - "traefik.http.middlewares.redirectLive.redirectregex.regex=^https?://live\\.cacic\\.com\\.br(/.*)?$"
      - "traefik.http.middlewares.redirectLive.redirectregex.replacement=https://cacic.com.br/live"
      - "traefik.http.middlewares.redirectLive.redirectregex.permanent=true"

      # Redirect cacic.dev.br para cacic.com.br
      - traefik.http.routers.devbr-redirect.rule=HostRegexp(`^(.+\.)?cacic\.dev\.br$`)
      - "traefik.http.routers.devbr-redirect.entrypoints=websecure "
      - "traefik.http.routers.devbr-redirect.tls=true"
      - "traefik.http.routers.devbr-redirect.priority=1000"
      - "traefik.http.routers.devbr-redirect.middlewares=devbr-to-combr"
      - "traefik.http.middlewares.devbr-to-combr.redirectregex.regex=^https://((?:.+)\.)?cacic\.dev\.br(/.*)?$$"
      - "traefik.http.middlewares.devbr-to-combr.redirectregex.replacement=https://$${1}cacic.com.br$${2}"
      - "traefik.http.middlewares.devbr-to-combr.redirectregex.permanent=true"

      # Redirect docs.fctapp.cacic.dev.br para docs.eventos.cacic.com.br
      - "traefik.http.routers.events-docs-redirect.rule=Host(`docs.fctapp.cacic.dev.br`)"
      - "traefik.http.routers.events-docs-redirect.entrypoints=websecure"
      - "traefik.http.routers.events-docs-redirect.tls=true"
      - "traefik.http.routers.events-docs-redirect.tls.certresolver=letsencrypt"
      - "traefik.http.routers.events-docs-redirect.priority=2000"
      - "traefik.http.routers.events-docs-redirect.middlewares=events-docs-redirect"
      - "traefik.http.middlewares.events-docs-redirect.redirectregex.regex=^https://docs\.fctapp\.cacic\.dev\.br(/.*)?$$"
      - "traefik.http.middlewares.events-docs-redirect.redirectregex.replacement=https://docs.eventos.cacic.com.br$${1}"
      - "traefik.http.middlewares.events-docs-redirect.redirectregex.permanent=true"

networks:
  traefik:
    external: true
  prometheus:
    external: true
